# Truth Social OAuth bearer token (required — API now returns 401 without it)
# To get one: register an app at truthsocial.com, complete OAuth flow, paste token here
TRUTH_SOCIAL_TOKEN=your_bearer_token_here

# Optional overrides
# POLL_INTERVAL_MINUTES=30
